Thanks you for the review on the CACK-pass project. This project is already in production and is live at https://cackpass.com but we have been having series of complain of pages too slow to load. The perfermance challenge made it necessary that we have to rework the entire project nad come up with something better than v1. Your task as a fullstack developer is to use the terms of reference below to provide an entire woeking version 2. here are the terms:

1. Performance: The new v2 should be blazingly fast. remove and fix whatever that inhibit speed / time to load a page.
2. Auth: NextAuth v5 + @auth/mongodb-adapter, HttpOnly-cookie sessions, every route authorizing off the session user id (this replaces the wallet-based identity when blockchain comes out)
3. Payments: Flutterwave (per-transaction fees only, no fixed cost), single idempotent fulfillment
4. DB: MongoDB Atlas M0 (free) — with the atomic inventory guard as the non-negotiable pattern
5. Mobile: Serwist PWA (PWA-first / Capacitor-later cost split)
6. Infra: Upstash Redis free tier (rate limiting), Resend free tier (email)
7. deployment: Vercel 
8. Image / color theme: Use same images as v1. maintain thesame color theme which is two major color of the logo as you can see in the css of v1.
9. UX: Provide a more modern UI/UX for v2.

For API keys, i will be providing them after you have completed the codes for the v2 nad i will add them to the .event
Whebever you need clarfication, you should ask and i will respond. 

Let build v2 now. 


























0xF00A2a5e8b0743049382bAeF9D7586e600D0D734

const PUBLIC_PAGES = [
  '/',
  '/events',
  '/payment',
  '/tickets',
  '/transactions',
  '/wallet',
  '/transfer',
  '/settings',
  '/create-ticket',
  '/events/[id]',  // Dynamic route pattern for individual event pages
  '/about',
  '/privacy',
  '/terms',
  '/payment/success',
  '/payment/failed'
]

